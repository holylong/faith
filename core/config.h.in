#ifndef FAITH_CONFIG_H
#define FAITH_CONFIG_H

#include <QtCore/qglobal.h>

#if defined(FAITH_LIBRARY)
#  define FAITH_EXPORT Q_DECL_EXPORT
#else
#  define FAITH_EXPORT Q_DECL_IMPORT
#endif

#define FAITH_VERSION "${PROJECT_VERSION}"
#define FAITH_PLUGIN_PATH "${RELATIVE_PLUGIN_PATH}"

#endif // FAITH_CONFIG_H
